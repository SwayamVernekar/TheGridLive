┌─────────────────────────────────────────────────────────────────────────┐
│                   F1 WEB APP - QUICK REFERENCE CARD                     │
└─────────────────────────────────────────────────────────────────────────┘

🚀 QUICK START (3 Commands)
════════════════════════════════════════════════════════════════════════════
cd f1-data-service && source venv/bin/activate && python python_server.py
cd backend && npm start
cd frontend && npm run dev

OR USE AUTOMATED SCRIPT:
./start-all.sh

📊 SERVICE PORTS
════════════════════════════════════════════════════════════════════════════
Python Data Service    http://localhost:5003
Node.js Backend        http://localhost:5002  
React Frontend         http://localhost:5173  ← Open this in browser

🏥 HEALTH CHECKS
════════════════════════════════════════════════════════════════════════════
curl http://localhost:5003/api/v1/health      # Python service
curl http://localhost:5002/api/health         # Backend (checks both)
curl http://localhost:5002/api/ping           # Backend only

🔌 API ENDPOINTS
════════════════════════════════════════════════════════════════════════════
GET /api/data/standings                        # Driver standings (Live)
GET /api/data/constructor-standings            # Team standings (Live)
GET /api/data/drivers                          # All drivers (Live)
GET /api/data/stats                            # Mock data (drivers/teams/news/legends)
GET /api/data/telemetry/:driver                # Driver telemetry
GET /api/data/schedule                         # Race calendar
GET /api/data/race-results/:year/:event        # Race results

📁 KEY FILES
════════════════════════════════════════════════════════════════════════════
f1-data-service/python_server.py               # Python Flask app
backend/server.js                              # Node.js Express app
frontend/src/api/f1Api.js                      # Centralized API helper
frontend/src/pages/DriverStandings.jsx         # Live standings UI
frontend/src/pages/Drivers.jsx                 # Live drivers UI
frontend/src/pages/ConstructorStandings.jsx    # Live teams UI
frontend/src/pages/F1Rewind.jsx                # Legendary drivers (API)
frontend/src/pages/PodiumPredictor.jsx         # Podium predictions (API)
frontend/src/pages/News.jsx                    # F1 news (API)

⚙️ CONFIGURATION FILES
════════════════════════════════════════════════════════════════════════════
backend/.env              # Backend config (MongoDB, Python API URL)
frontend/.env             # Frontend config (Backend API URL)

Example backend/.env:
PORT=5002
MONGO_URI=mongodb://localhost:27017/f1app
PYTHON_API_URL=http://localhost:5003/api/v1

Example frontend/.env:
VITE_API_URL=http://localhost:5002

🐛 TROUBLESHOOTING
════════════════════════════════════════════════════════════════════════════
Problem: Port in use
Solution: lsof -i :<PORT> then kill -9 <PID>

Problem: Module not found (Python)
Solution: source venv/bin/activate && pip install -r requirements.txt

Problem: FastF1 no data
Solution: rm -rf f1-data-service/cache OR use ?year=2024 in requests

Problem: CORS error
Solution: Ensure all three services are running on correct ports

Problem: MongoDB error
Solution: brew services start mongodb-community OR use Atlas cloud

📚 DOCUMENTATION
════════════════════════════════════════════════════════════════════════════
README.md              # Main documentation
SETUP_GUIDE.md         # Detailed setup instructions
DELIVERABLES.md        # Complete project summary
IMAGE_STRATEGY.md      # Image handling approach

🛠️ DEVELOPMENT COMMANDS
════════════════════════════════════════════════════════════════════════════
# Python service
cd f1-data-service
python python_server.py              # Start service
pip freeze > requirements.txt        # Update dependencies

# Backend
cd backend
npm start                           # Production mode
npm run dev                         # Development (with nodemon)

# Frontend
cd frontend
npm run dev                         # Development server
npm run build                       # Production build
npm run preview                     # Preview production build

🔍 DEBUGGING
════════════════════════════════════════════════════════════════════════════
# View logs
Python: Check terminal running python_server.py
Backend: Check terminal running npm start
Frontend: Open browser DevTools Console (F12)

# Test endpoints manually
curl http://localhost:5003/api/v1/standings?year=2024
curl http://localhost:5002/api/data/drivers
curl http://localhost:5002/api/health

# Check if services are running
lsof -i :5003  # Python
lsof -i :5002  # Backend
lsof -i :5173  # Frontend

📦 DEPENDENCIES
════════════════════════════════════════════════════════════════════════════
Python:   Flask, FastF1, Pandas, Flask-CORS
Backend:  Express, Mongoose, CORS, dotenv
Frontend: React, Vite, Framer Motion, Recharts, Lucide

🎯 DATA FLOW
════════════════════════════════════════════════════════════════════════════
User → React (5173) → Node.js (5002) → Python (5003) → FastF1 → F1 Data
                       ↑___ MongoDB ___↑

🖼️ IMAGE STRATEGY
════════════════════════════════════════════════════════════════════════════
Using Unsplash dynamic URLs:
https://source.unsplash.com/400x400/?formula1,racer,portrait,{driver.number}

Benefits: Zero config, high quality, no costs, dynamic variety

🚢 DEPLOYMENT CHECKLIST
════════════════════════════════════════════════════════════════════════════
□ Python service → Heroku/Railway/Render
□ Backend → Heroku/Railway/Vercel/AWS
□ Frontend → Vercel/Netlify/AWS S3
□ Update environment variables for production URLs
□ Use MongoDB Atlas for cloud database
□ Set PYTHON_API_URL to deployed Python service
□ Set VITE_API_URL to deployed backend

⚡ PERFORMANCE TIPS
════════════════════════════════════════════════════════════════════════════
• First run downloads data (slow) - subsequent runs use cache
• Clear cache to refresh data: rm -rf f1-data-service/cache
• Use year parameter for specific seasons: ?year=2024
• Python service caches data automatically in cache/ directory

🆘 EMERGENCY RESTART
════════════════════════════════════════════════════════════════════════════
# Kill all services
pkill -f python_server.py
pkill -f "node.*server.js"
pkill -f "vite"

# Restart all
./start-all.sh

💡 PRO TIPS
════════════════════════════════════════════════════════════════════════════
• Use ./start-all.sh for automatic startup
• Keep terminals visible to monitor logs
• Use ?year=2024 if current season has no data yet
• MongoDB is optional for F1 data features
• Browser DevTools Network tab shows API calls
• Python cache speeds up subsequent requests significantly

🎮 FEATURE STATUS
════════════════════════════════════════════════════════════════════════════
✅ Driver Standings (live data)
✅ Constructor Standings (live data)
✅ Drivers List (live data)
✅ Driver Details (live data)
✅ Telemetry API (available)
✅ Race Schedule (available)
✅ Race Results (available)
⏳ User favorites (MongoDB needed)
⏳ Authentication (future enhancement)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                    READY TO RACE! 🏎️💨
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
